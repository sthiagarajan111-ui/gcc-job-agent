Update the existing file: src/dashboard.js

Read the current src/dashboard.js first before making any changes.

═══════════════════════════════════════════════════════
FIX 1 — REORDER STATS BAR (ROW 1)
═══════════════════════════════════════════════════════

The stats bar at the top of the dashboard currently
shows stats in wrong order. Reorder to:

1. Total Jobs (All Time) — white/grey
2. Today's New Jobs — cyan
3. Fortune 500 — gold #D29922
4. Tier 1 — green #3FB950
5. Tier 2 — blue #58A6FF
6. Applied — purple #8957E5

Each stat box shows:
  Large number (bold, 24px)
  Label below (small, 12px, grey)

Style each with matching left border color.

═══════════════════════════════════════════════════════
FIX 2 — STATS BAR UPDATES WITH CANDIDATE FILTER
═══════════════════════════════════════════════════════

When the Candidate filter is changed (filter-candidate),
ALL stat counters must update to reflect ONLY the
jobs for the selected candidate.

For example:
  If "Dheeraj Thiagarajan" selected:
    Total Jobs = count of DT jobs only
    Today's New = count of today's DT jobs only
    Fortune 500 = count of Fortune 500 DT jobs only
    Tier 1 = count of Tier 1 DT jobs only
    Tier 2 = count of Tier 2 DT jobs only
    Applied = count of applied DT jobs only

  If "All Candidates" selected:
    Show counts for all jobs combined

Create function updateStatsBar(filteredJobs):
  Counts stats from the filteredJobs array passed in
  Updates all 6 stat box numbers in the DOM

Call updateStatsBar() every time applyFilters() runs
by passing the filtered jobs array to it.

Also call updateStatsBar(ALL_JOBS) on initial page load.

═══════════════════════════════════════════════════════
FIX 3 — MOVE RESET FILTERS AND ADD JOB TO ROW 2
═══════════════════════════════════════════════════════

Currently the layout is:
  Row 1: Stats bar
  Row 2: Filter dropdowns (Candidate, Tier, Company, Source, Location, Role, Fortune500, Sort, Experience, Date)
  Row 3: Search box | Reset Filters | + Add Job from URL
  Row 4: Job cards grid

Change to:
  Row 1: Stats bar
  Row 2: Filter dropdowns + Reset Filters button + Add Job from URL button
          (all on the same row, Reset and Add Job on the RIGHT side)
  Row 3: Search box (full width)
  Row 4: Job cards grid

For Row 2, use flexbox with:
  Left side: all filter dropdowns (wrap if needed)
  Right side: Reset Filters + Add Job buttons
  align-items: center

The filter dropdowns should wrap naturally on smaller screens.
Reset Filters button: same style as before (grey/outlined)
Add Job button: same style as before (green with + icon)

Remove the old Row 3 that had Reset and Add Job.
Keep the Search box as its own full-width row.

═══════════════════════════════════════════════════════
FIX 4 — TRACKER PAGE CANDIDATE FILTER
═══════════════════════════════════════════════════════

In the GET /tracker route HTML:

Add a Candidate filter at the TOP of the Tracker page,
above the Kanban columns:

Style: same pill/toggle style as main dashboard
Options:
  All Candidates (default)
  🟡 Dheeraj Thiagarajan
  🟢 Thiagarajan Shanthakumar

When candidate is selected:
  Filter ALL Kanban columns to show only cards
  for the selected candidate

  Each application card in the Kanban should have
  a candidateId field (from the applications.json data)

  Show/hide cards based on selected candidate:
  cards.forEach(card => {
    const cardCandidate = card.dataset.candidateId
    if (selectedCandidate === 'all' ||
        cardCandidate === selectedCandidate) {
      card.style.display = 'block'
    } else {
      card.style.display = 'none'
    }
  })

Also update each Kanban column header to show
the count of VISIBLE cards (not total):
  "Applied (3)" updates to "Applied (1)" when filtered

Add candidateId to each Kanban card display:
  Small colored dot next to job title
  Yellow dot = Dheeraj, Green dot = Thiagarajan

In src/appTracker.js or the tracker data:
  When a job is added to tracker via APPLY & TRACK,
  include the candidateId from the job object
  Store in applications.json as candidateId field

In POST /api/add-application route:
  Accept candidateId in the request body
  Store it in the application record

In the APPLY & TRACK button onclick in buildCard():
  Pass job.candidateId when calling addToTracker()

═══════════════════════════════════════════════════════
TEST AND CONFIRM
═══════════════════════════════════════════════════════

After all changes:
  node src/dashboard.js

Confirm:
  GET / → 200, stats in correct order
  Stats update when candidate filter changes
  Reset Filters and Add Job on Row 2 with filters
  GET /tracker → 200, candidate filter at top
  Kanban cards show candidate dot badges

Print: "Dashboard layout v2 complete"
Print: "Stats bar order: Total, Today, Fortune500, T1, T2, Applied"
Print: "Tracker candidate filter: YES"
Print: "Reset+AddJob moved to filter row: YES"
